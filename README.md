cannot be any simpler,
just run the following command:
```console
python blabla_crawler.py
```
and it should start doing it's thing, it searches from id 1 to 600 by default, you can change it by modifying the last line call "scrape_nikkes"

might prompt you to install selenium, to do this please run
```console
pip install selenium
```
